[project]
name = "ml"
version = "0.1.0"
description = "IOS app that can classify food"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "executorch>=1.3.1",
    "torch>=2.12.0",
    "torchvision>=0.27.0",
]
